{
  "name": "global-disaster-managing-web",
  "version": "1.0.0",
  "description": "Global Disaster Managing Web - Earth-Scale Multi-Hazard Intelligence & 500m Early Warning System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "disaster-management",
    "early-warning",
    "gis",
    "sih2026"
  ],
  "author": "SIH Team",
  "license": "MIT"
}
